import React from 'react';
import TopBar from "../components/TopBar";

const ProductsPage = () => {
    return (
        <div>
            <TopBar />
            products
        </div>
    );
};

export default ProductsPage;